Start the BTC AI Agent development server.

```bash
cd /Users/shashankreddyganta/Documents/btc-ai-agent && uv run uvicorn btc_agent.web.app:app --reload --port 8000
```

The dashboard will be available at http://localhost:8000
Use this for local development and testing UI changes.
